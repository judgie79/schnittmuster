require("ts-node/register/transpile-only");
require("tsconfig-paths/register");
const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "config", "config.js"),
  modelsPath: path.resolve(__dirname, "src", "features"),
  migrationsPath: path.resolve(
    __dirname,
    "src",
    "infrastructure",
    "database",
    "migrations"
  ),
  seedersPath: path.resolve(
    __dirname,
    "src",
    "infrastructure",
    "database",
    "seeders"
  ),
};
