export {
  HydrationBoundary,
  QueryClient,
  QueryClientProvider,
  dehydrate,
  hydrate,
} from '@tanstack/react-query';

export type {
  HydrationBoundaryProps,
  QueryClientConfig,
} from '@tanstack/react-query';
